{
  "name": "form",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "parcel index.html --no-source-maps --open --public-url /dist",
    "prod": "parcel index.html --no-source-maps --no-cache --public-url /dist --dist-dir dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0-beta.3.1",
    "parcel": "^2.0.0-beta.3.1"
  }
}